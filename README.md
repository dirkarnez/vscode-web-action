vscode-web-action
=================
### Useful commands
- `docker container exec ${ container-id } bash`, and then run `.sh` files inside the bash
- `docker container exec -it ${ container-id } sh -c "echo 123"`
